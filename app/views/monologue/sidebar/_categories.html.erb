<%= sidebar_section_for(t(".title"), 'categories') do %>
   <ul>
     <% @tags.each do |tag| %>
       <li id="tag"><%=link_to "#{tag.name} (#{tag.posts_count})", tag_url(tag) %></li>
     <% end %>
   </ul>
 <% end %>
