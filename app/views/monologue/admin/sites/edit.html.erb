<h1><%= t(".header") %></h1>
<%= monologue_admin_form_for [:admin, @site], method: :put do |site| %>
  <%= render partial: 'form', locals: { site: site } %>
  <%= site.submit t(".save"), class:  "btn btn-large btn-primary" %>
<% end %>